#include "funcHelp.h"

/*
int average(int rrIntervals[]){
	int avg = 0;
	int sum = 0;
	for(int i = 0; i < sizeOf(rrIntervals); i++){
		sum += rrIntervals[i];
	}
	avg = sum/sizeOf(rrIntervals);
	return avg;
}
*/
